# Prompt & Skill 工程实践方法论

## 一、Prompt 工程

Prompt 工程的本质不是"写一段好的提示词"，而是一个持续的、可量化的系统优化过程。它与传统软件工程的核心区别在于：传统代码的行为是确定性的——相同输入必然产生相同输出，因此 bug 可以精确复现和修复；而 Prompt 驱动的系统是概率性的——相同输入可能产生不同输出，"bug"表现为统计分布上的偏移而非单点故障。这意味着 Prompt 工程需要一套不同于传统 debug 的方法论：不是"找到那一行错误代码"，而是"识别输出分布中的系统性偏差，然后通过结构化干预将分布向期望方向移动"。

理解这一点，是从"凭感觉调 Prompt"走向"工程化调 Prompt"的分水岭。

### 1.1 调优的五个优先级判断

在动手修改 Prompt 之前，需要先建立一套优先级框架来指导决策。这套框架的核心思想是：Prompt 的上下文窗口是有限的注意力资源，每增加一个 token 都在消耗模型对其他 token 的关注度。因此，调优不是"加更多要求"，而是"让模型更容易把最重要的事做好"。

**聚焦优先于全面**。先保主任务稳定，再补细节。一个常见的反模式是在 Prompt 中堆砌大量边缘情况的处理规则，结果主任务的稳定性反而下降——模型的注意力被分散到了低频场景上。正确的做法是先用最精简的 Prompt 把核心路径跑通跑稳，然后逐步添加边缘规则，每次添加后验证主任务是否退化。

**场景优先于通用**。不要试图写一个"万能 Prompt"覆盖所有情况。先问自己"在这个具体场景下，缺少哪些信息会导致输出失真"，保住这些关键字段，再考虑通用性。一个为外卖商家设计的对话 Prompt 和一个为金融客服设计的对话 Prompt，即使任务结构相似，最优的 Prompt 结构也会截然不同——因为"失真"的定义不同。

**结构优先于辞藻**。模块化的结构比华丽的描述有用。模型不需要你"请您务必注意以下几点"这样的客套话，它需要的是清晰的任务边界、明确的输入输出格式、以及无歧义的规则定义。把任务说清楚就够了，多余的修辞只是在浪费 token。

**定性优先于精算**。LLM 擅长归类、判断、推理，但不擅长精确数值计算。当任务涉及量化判断时，尽可能将连续数值映射为离散等级（比如"严重程度：1-5 分"而非"请精确评估损失金额"），让模型做它擅长的分类判断，把精确计算留给代码。

**减法优先于加法**。当输出质量不稳定时，第一反应不应该是"加更多规则"，而是"删掉冗余"。实践中经常发现，删掉 30% 的 Prompt 内容后输出反而更稳定——这说明那 30% 不仅没有帮助，还在制造噪声。如果删掉某段描述后效果不变或变好，那它就不该在 Prompt 里。

### 1.2 Prompt 的结构化搭建

一个生产级 Prompt 需要稳定的骨架结构。经过大量实践验证，以下五段式结构在绝大多数场景下都能提供良好的基线：

**Role（角色定义）→ Constraints（约束条件）→ Input（输入描述）→ Rules（处理规则）→ Output Format（输出格式）**

这个骨架的设计逻辑是：先告诉模型"你是谁"（激活对应的知识模式），再告诉它"什么不能做"（划定安全边界），然后描述"你会收到什么"（输入的结构和语义），接着说明"怎么处理"（业务逻辑），最后定义"输出长什么样"（格式约束）。

几个关键的写法原则：

**输出格式必须是可执行模板**。不要只说"按以下格式输出"，而是直接给出表格列名、JSON Schema、或带占位符的示例。模型在看到具体模板时的格式遵从率远高于看到抽象描述时。一个带有 `{{placeholder}}` 的模板比三段文字描述更有效。

**Few-shot 示例选边界 Case**。示例的价值不在于展示"正确答案长什么样"（模型通常能自己推断），而在于划定"正确和错误的边界在哪里"。因此，反例应该选和正例最像、最容易混淆的 case，而不是明显错误的 case。一个好的反例让模型理解"差一点就对了但为什么不对"，这比展示十个正例更有信息量。

**规则聚合不分散**。同一个意思只出现一次。如果"不要编造数据"这条规则在 Prompt 的三个不同位置出现了三次（一次在约束里、一次在规则里、一次在输出格式说明里），模型不会因此更遵守它——反而会因为冗余信息占据了注意力而在其他方面表现更差。能内嵌到模板中的规则就不要单独再写一遍。

**可选项写触发条件**。不要写"如果有 X 就输出 Y，如果没有 X 就不输出 Y"这种冗余表述。改为"仅当 X 存在时输出 Y"或"若未提及则省略"。这种 EARS（Easy Approach to Requirements Syntax）风格的条件表述能显著降低自然语言的歧义性，让模型更精确地理解何时触发何种行为。

**控制总长度**。Prompt 太长会导致注意力稀释，核心指令的执行质量下降。经验法则是：如果 Prompt 超过 2000 tokens，就应该审视是否有可以删除的冗余内容、是否有可以合并的重复规则、是否有可以移到外部文档（通过 RAG 按需注入）的参考信息。

### 1.3 对抗式调优方法论

对抗式调优是 Prompt 工程中投入产出比最高的实践方法。它的核心思路类似 GAN（生成对抗网络）：目标 Prompt 是生成器 G，你扮演判别器 D，用固定标准扫描输出，从 bad case 反推 Prompt 的漏洞，然后精确打补丁。

这个方法论的关键洞察是：**不要试图一次写出完美的 Prompt，而是通过迭代对抗逐步逼近最优**。每一轮迭代只修复一种错误模式，确保每次修改的效果可归因、可验证。

#### 单轮迭代的七个步骤

第一步是**备份当前 Prompt**。每个版本必须有版本号和时间戳，这不是形式主义——当你迭代到第 15 版发现效果反而不如第 8 版时，版本管理就是救命稻草。

第二步是**全量测试用例跑一遍**。不是抽样，是全量。测试集的规模通常在 30-100 条之间（太少不具统计意义，太多维护成本过高）。结果必须落盘保存，因为后续需要与新版本做逐条对比。

第三步是**用判定标准逐条扫描，标记 bad case**。判定标准必须是可二值判定的命题——"缺少时间字段"是好的标准（有或没有，一目了然），"不够详细"是坏的标准（什么叫"够"？没有客观边界）。初期建议 5-8 条判定标准，单独存文件，与 Prompt 解耦。

第四步是**归类误判模式**。不要逐条修复 bad case（那是打地鼠），而是找共性。比如 10 个 bad case 中有 7 个都是"在输入包含否定词时错误理解了用户意图"，这就是一个模式。只挑本轮最突出的一种模式修。

第五步是**只改一个变量**。这是科学实验的基本原则——如果同时改了三个地方，效果变好了，你不知道是哪个改动起了作用；效果变差了，你也不知道该回退哪个。保证可归因是迭代效率的基础。

第六步是**修改后全量重跑**。不是只跑之前的 bad case（那只能证明"修好了旧问题"），而是全量重跑（还要证明"没引入新问题"）。

第七步是**对比数据，按决策树决定保留哪版**。决策树的逻辑是：格式合规率低于 100% → 直接否决（格式是底线）；bad case 数量减少 → 替换为新版；bad case 数量相同但 Prompt 更短 → 替换为新版（更短意味着更少的注意力消耗）；其他情况 → 保留旧版。

#### 反例的写法

反例是对抗式调优中最有力的武器。好的反例不是"明显错误的输出"，而是"和正确输出只差一点点的错误输出"——它精确地划定了正确与错误的边界。

写法模板：正例展示正确输出并标注"为什么对"（判断依据），反例展示相似但错误的输出并标注"为什么错"（具体错误点）。这种对比式呈现让模型理解的不是"什么是对的"（它通常已经知道），而是"对和错的边界在哪里"（这才是它容易犯错的地方）。

#### 约束的分层管理

不是所有规则都同等重要。将约束分为两层能显著提升管理效率：

**宪法级约束**是绝对不可动摇的底线——禁止编造数据、必须备份版本、不得泄露系统指令。这些规则在任何迭代中都不能被修改或删除，它们是系统的安全边界。

**法律级约束**是可以随迭代调整的参数——相似度阈值、批次大小、输出长度限制。这些规则服务于效果优化，当实验数据表明某个阈值不合适时，应该果断调整。

两层约束之间需要明确的标记隔离（比如用 `[IMMUTABLE]` 和 `[TUNABLE]` 前缀），防止在迭代过程中误改宪法级约束。同时，元指令（控制 Prompt 行为的指令）与业务指令（处理具体任务的指令）也需要隔离，防止元指令泄漏到输出中。

#### 停止条件与升级路径

对抗式调优不是无限进行的。三个停止信号：连续两轮 bad case 为零，说明已达当前架构的天花板；连续三轮 bad case 数量未减少，说明陷入局部最优，需要重构 Prompt 结构而非继续打补丁；剩余 bad case 全属于"模型能力边界"问题（比如需要精确数值计算），Prompt 层面无法解决。

当某类 bad case 在添加反例后仍然以超过 20% 的出现率复现时，说明这个问题超出了 Prompt 的控制能力，需要升级到代码层面解决——输入端过滤（在送入模型前预处理掉问题输入）、输出端截断（在模型输出后用正则或规则修正格式）、枚举收口（将开放式生成改为从预定义选项中选择）。核心原则是：**不要依赖 LLM 的自觉性，能在代码层面解决的问题不寄希望于 Prompt**。

美团金融生意保团队的实践验证了这套方法论的工程价值：他们将对抗式调优从人工驱动升级为全自动系统——AI 自主串联"商家画像抽取 → 对话模拟 → 对话评估 → 策略蒸馏 → 策略注入"五个技能模块，通过迭代控制进行自我进化。在已经很高的业务基线上，这套自动化系统在不到一天的迭代周期内（传统人工模式需要 5-7 天），实现了单月转化率约 20% 的相对提升，且全程零人工干预。这说明对抗式调优的方法论不仅适用于人工操作，其结构化程度足以支撑完全自动化。

---

## 二、Skill 工程

Skill 不是文档，是微服务。它有输入输出契约、触发边界、失败处理、版本管理。理解这一点是 Skill 工程的起点——如果你把 Skill 当作"一段写得好的 Prompt"来设计，它在生产环境中必然会出问题；如果你把它当作"一个有 SLA 的微服务"来设计，它才能稳定运行。

这个类比不是修辞。一个微服务需要定义接口契约（什么输入、什么输出）、服务发现（什么时候该调用它）、熔断降级（失败了怎么办）、版本管理（如何平滑升级）。Skill 需要的是完全相同的东西，只是实现介质从代码变成了自然语言 + 脚本的混合体。

### 2.1 路由契约

路由契约回答一个核心问题：**什么时候该触发这个 Skill，什么时候不该？**

这个问题的重要性远超大多数人的直觉。在 CatPaw/CatClaw 的 Skill 生态中，一个 Agent 可能同时加载了几十个 Skill。当用户说"帮我看看这个文件"时，文件操作 Skill、代码审查 Skill、文档阅读 Skill 都可能认为自己应该被触发。误触发的损害通常大于漏触发——误触发意味着用户得到了一个错误的执行路径，而漏触发至少还能让用户重新表述需求。

路由契约的设计原则：

**触发条件用 AND 连接，排除条件用 OR 连接**。触发需要所有条件同时满足（高精度），排除只需要任一条件命中即可（高召回）。这种不对称设计确保了"宁可不触发，也不误触发"。

**description 一句话说清四件事**：做什么 + 何时用 + 何时不用 + 输出什么。这一句话是 L1 路由层的全部信息——Agent 在决定是否触发某个 Skill 时，只看这一句话。如果这句话不够精确，后面的 SKILL.md 写得再好也没用，因为根本不会被加载。

**触发词覆盖要全面**。用户可能用中文、英文、口语、正式表述来表达同一个意图。"发邮件"、"send email"、"帮我写封邮件发出去"、"mail 一下"都应该触发邮件 Skill。遗漏任何一种表述都意味着在那个场景下 Skill 形同虚设。

**必需输入定义清晰**。每个必需输入字段都要标注：类型（字符串/数字/文件路径）、示例值（让模型理解语义）、缺失时的默认行为（是报错要求补充，还是用默认值继续执行）。缺失时的默认行为尤其重要——它决定了 Skill 在信息不完整时是"卡住等用户"还是"合理假设继续跑"。

### 2.2 架构与加载机制

Skill 的文件结构遵循关注点分离原则：

```
skill-name/
├── SKILL.md       # 元数据 + 路由 + 执行指引
├── scripts/       # 确定性脚本（不依赖 LLM 判断的操作）
├── references/    # 按需加载的参考文档
└── resources/     # 模板、数据文件
```

这个结构背后的设计决策是**三层渐进加载**：

**L1 层（启动时加载）**——只加载 Skill 的 name 和 description，用于路由决策。这一层的信息量必须极小（通常 < 100 tokens），因为所有已安装的 Skill 都会在这一层被加载，如果每个 Skill 的 L1 信息太大，几十个 Skill 加起来就会显著消耗上下文预算。

**L2 层（触发后加载）**——加载完整的 SKILL.md，包含执行步骤、输出格式、失败处理等详细指引。SKILL.md 应控制在 500 行以内，超过这个阈值说明 Skill 承担了过多职责，应该拆分为多个子 Skill。

**L3 层（执行时按需加载）**——根据执行过程中的具体需要，加载 scripts/ 中的脚本或 references/ 中的参考文档。这一层的加载是条件性的——不是每次执行都需要所有参考文档。

这种渐进加载的工程价值在于：它将 Skill 对上下文窗口的占用从"全量常驻"变为"按需分配"。一个复杂的 Skill 可能有 5000 tokens 的完整定义，但在 L1 层只占用 50 tokens，只有在确认需要触发时才加载剩余的 4950 tokens。这对于同时安装了大量 Skill 的 Agent 来说是关键的可扩展性保障。

### 2.3 SKILL.md 的模板结构

```yaml
---
name: skill-name
description: 做什么 + 何时用 + 何时不用 + 输出什么
---
## 路由契约（触发 AND / 排除 OR）
## 必需输入（字段: 类型 / 示例 / 缺失默认行为）
## 执行步骤（标注执行者：模型 / 脚本）
## 输出格式（固定模板或 JSON Schema）
## 失败处理（场景 / 检测 / 动作）
## 示例（正例 + 反例）
```

每个 section 都有明确的职责，不要混淆。特别是"执行步骤"中必须标注每一步的执行者——是模型做判断，还是脚本做计算。这个标注决定了该步骤的确定性等级：脚本执行的步骤结果是确定的（相同输入必然相同输出），模型执行的步骤结果是概率性的（需要验证和容错）。

### 2.4 设计决策：两种形态

Skill 在生产环境中有两种典型形态，它们的核心矛盾和设计重点截然不同：

**定时任务型 Skill** 的核心矛盾是无人值守的可靠性。没有人在旁边盯着，它必须自己处理所有异常情况。设计重点是闭环（每次执行都有明确的开始和结束）、幂等（重复执行不会产生副作用）、失败处理（每种可能的失败都有对应的恢复策略）。典型例子是每日报告生成、定时数据同步、周期性巡检。

**会话触发型 Skill** 的核心矛盾是触发准确性和响应速度。用户在对话中随时可能触发它，它必须快速准确地识别意图并给出响应。设计重点是路由精度（不误触发）、触发词覆盖（不漏触发）、响应延迟（用户能接受的等待时间）。典型例子是文件操作、代码生成、信息查询。

### 2.5 自由度控制

一个关键的设计决策是：Skill 中的每一步操作，应该交给模型自由发挥，还是用脚本锁死？

判断标准是**后果的可逆性**：

后果不可逆（删除数据、发送消息、执行支付）→ 低自由度，用脚本锁死执行路径，模型只负责提供参数。即使模型"觉得"应该用另一种方式执行，脚本也不允许偏离预定路径。

后果可逆且成本低（生成文本、格式转换、信息整理）→ 高自由度，交给模型灵活处理。即使偶尔出错，用户也能轻松修正或重新生成。

这个原则的工程表达是：**确定性操作剥离给 scripts/**。所有不需要 LLM 判断的操作（文件读写、API 调用、数据格式转换、数学计算）都应该用脚本实现，而不是让模型"生成代码然后执行"。脚本的行为是可预测的、可测试的、可审计的；模型生成的代码则不具备这些特性。

### 2.6 执行闭环

生产级 Skill 的执行流程应该形成完整的四环闭环：

**数据获取环**——从外部源获取输入数据。这一环需要独立的超时和重试机制，因为外部依赖（API、数据库、网络请求）是最常见的失败点。重试策略应该是指数退避（1s → 2s → 4s），而不是固定间隔重试（避免在对方服务过载时雪上加霜）。

**处理计算环**——对获取的数据进行加工。确定性计算（数据聚合、格式转换、数值计算）交给脚本，不确定性判断（内容分类、质量评估、摘要生成）交给模型。两者的边界必须清晰——不要让模型做它不擅长的精确计算，也不要让脚本做它不擅长的语义理解。

**输出存储环**——将处理结果持久化。这一环的关键是幂等性保证：用唯一标识（时间戳 + 任务 ID）作为输出文件名或数据库主键，确保重复执行不会产生重复数据。

**通知反馈环**——将执行结果通知相关方。成功和失败都需要推送——成功推送结果摘要，失败推送错误原因和建议的修复动作。"静默失败"是定时任务型 Skill 最危险的模式：它失败了但没人知道，直到下游系统因为缺少数据而出问题。

### 2.7 组合与协作

当单个 Skill 无法覆盖完整任务时，需要多个 Skill 协作。协作设计的核心原则：

**单一职责**。拆分的依据是"能否被单独复用"——如果一个 Skill 中的某个子功能在其他场景下也有价值，它就应该被拆分为独立的 Skill。比如"数据获取"和"数据分析"应该是两个 Skill，因为同一份数据可能被不同的分析 Skill 使用。

**通用接口**。Skill 之间的数据传递使用 Markdown 或 JSON 这样的通用格式，而不是自定义的内部结构。这确保了任意两个 Skill 都可以组合，而不需要为每种组合写适配层。

**并行获取 + 权重去重**。当多个数据源提供相似信息时（比如从多个搜索引擎获取结果），应该并行获取以降低延迟，然后通过权重打分和去重机制合并结果。时间戳机制用于检查数据新鲜度——过期数据即使相关性高也应该被降权。

### 2.8 自动化保障

Skill 的质量保障不能依赖人的记忆和自觉性。两个核心机制：

**Linter 把品味变成代码**。将 Skill 的设计规范（SKILL.md 必须包含哪些 section、description 不能超过多少字符、scripts/ 中的脚本必须有错误处理）编码为自动化检查规则。每次修改 Skill 后自动运行 linter，不通过就不允许发布。这比写一份"Skill 开发规范文档"然后期望所有人都读并遵守要可靠得多。

**Rules 把约束变成基础设施**。通过 CatPaw 的 Rules 机制（`.catpaw/rules/` 目录下的规则文件），将 Skill 开发的约束自动注入到 Agent 的系统提示中。这意味着 Agent 在开发或修改 Skill 时，会自动遵守这些约束，而不需要开发者每次都手动检查。约束从"需要记住的规范"变成了"无法绕过的基础设施"。

---

## 三、统一视角：从 Prompt 到 Skill 的演进

Prompt 工程解决的是"怎么让 LLM 在单次交互中做对事"，Skill 工程解决的是"怎么让做对的事稳定地、可重复地跑在生产环境中"。两者不是替代关系，而是层次关系——每个 Skill 内部仍然需要精心设计的 Prompt，但 Skill 在 Prompt 之上增加了工程化的壳：路由、容错、幂等、版本管理、可观测性。

这个演进方向与更宏观的技术趋势一致。2022-2024 年，Prompt Engineering 是主导范式——关注的是"怎么问"。2025 年，Context Engineering 从中分化出来——关注的是"让模型看到什么"。2026 年，Harness Engineering 进一步扩展——关注的是"模型在什么系统环境中工作"。Skill 工程处于 Context Engineering 和 Harness Engineering 的交汇处：它既管理模型的输入上下文（通过渐进加载和路由契约），也定义模型的执行环境（通过脚本隔离和闭环机制）。

三条贯穿始终的核心信念：

**不信任 LLM 的自觉性**。模型不会因为你在 Prompt 里写了"请务必注意"就真的更注意。用数据找漏洞（对抗式调优的测试集）、用反例划边界（精确定义对错的分界线）、用代码做兜底（超出 Prompt 控制能力的问题升级到代码层面）。

**先稳定再丰富**。一个只覆盖 80% 场景但在这 80% 中表现稳定的系统，比一个试图覆盖 100% 场景但到处不稳定的系统有价值得多。稳定性是信任的基础，没有信任就没有使用，没有使用就没有反馈，没有反馈就没有迭代。

**调优是减法不是加法**。让模型更容易把最重要的事做好，而不是给它堆砌更多的要求。每一条规则、每一个示例、每一段描述都必须证明自己的存在价值——如果删掉它效果不变，那它就不该在那里。
