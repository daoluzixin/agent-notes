# RAG 工程笔记：从检索到生成的全链路设计

## 一、概述与全链路

检索增强生成（Retrieval-Augmented Generation）的核心命题并不复杂：让大模型在回答问题时，先从外部知识库中找到相关证据，再基于证据生成回答。这个思路最早由 Facebook AI Research 在 2020 年的论文中正式提出，但真正让它成为工程标配的，是 2023 年之后大模型落地过程中暴露出的三个系统性缺陷——知识时效性断裂、私域数据缺失、以及模型幻觉无法自愈。

RAG 的工程价值在于它提供了一条不需要重新训练模型就能注入新知识的路径。相比微调（Fine-tuning），RAG 的优势是知识更新成本低（改文档而非改权重）、可解释性强（能追溯到具体来源段落）、数据安全可控（私域数据不需要上传到第三方训练平台）。但这些优势的兑现，取决于整条链路中每个环节的工程质量。

一条典型的 RAG 链路包含五个阶段，每个阶段都有独立的失败模式：

**文档处理阶段**负责将原始知识（PDF、Word、网页、代码仓库）转化为可检索的结构化单元。这里的核心挑战是信息密度与语义完整性的平衡——切得太细会丢失上下文，切得太粗会引入噪声。美团内部的实践表明，文档处理阶段的质量直接决定了后续所有环节的上限，一个糟糕的分块策略无法被再好的 Embedding 模型或 Rerank 模型挽救。

**Embedding 阶段**将文本块转化为高维向量表示。这一步的关键决策包括模型选择（通用模型 vs 领域微调模型）、向量维度（维度越高表达力越强但存储和检索成本也越高）、以及是否需要对查询和文档使用不同的编码策略。

**检索阶段**从向量数据库中召回候选文档。单纯的向量相似度搜索在很多场景下不够用——它擅长捕捉语义相似性，但对精确关键词匹配（如产品编号、人名、错误码）表现不佳。因此混合检索（向量 + 关键词）成为生产环境的标准配置。

**重排序阶段**对召回的候选文档进行精细化排序。向量检索是一种"粗筛"，它用 ANN（近似最近邻）算法在毫秒级完成百万级文档的初筛，但牺牲了精度。Rerank 模型则是"精排"，它用交叉编码器（Cross-Encoder）对 query-document 对进行逐一打分，计算成本高但排序质量显著优于向量余弦相似度。

**生成阶段**将检索到的上下文与用户问题一起送入大模型生成最终回答。这里的工程挑战包括上下文窗口管理（如何在有限 token 预算内放入最有价值的信息）、Prompt 设计（如何引导模型基于证据回答而非自由发挥）、以及幻觉检测（如何判断模型的回答是否忠实于检索到的证据）。

这五个阶段形成了一条端到端的质量传导链：上游的信息损失会在下游被放大，而下游的优化无法弥补上游的缺陷。理解这一点，是做好 RAG 工程的前提。

---

## 二、文档处理与分块策略

文档处理是 RAG 系统中最容易被低估的环节。很多团队把精力集中在模型选择和检索算法上，却忽略了一个基本事实：如果知识库中的文档块本身质量不高，后续所有环节都在"精心检索垃圾"。

### 2.1 文档解析

原始文档的格式多样性是第一个工程挑战。PDF 文档可能包含扫描图片（需要 OCR）、复杂表格（行列关系容易在纯文本提取中丢失）、多栏排版（阅读顺序不等于物理顺序）。Word 文档的样式层级（标题、正文、列表）是重要的结构信号，但很多解析工具会丢弃这些信息。代码文件有自己的语法结构（函数、类、模块），按行切分会破坏语义单元。

生产环境中的文档解析通常需要一个多格式适配层：针对不同文件类型使用不同的解析策略，并在解析结果中保留尽可能多的结构元数据（标题层级、页码、表格坐标、代码函数签名等）。这些元数据在后续的检索和展示阶段都有价值。

### 2.2 分块策略

分块（Chunking）是文档处理的核心决策点。它的本质是在两个矛盾目标之间寻找平衡：

- **语义完整性**要求每个块包含足够的上下文，使其在脱离原文后仍然可以被理解。一个只包含"如上所述，该方案的优势在于..."的块，脱离了前文就毫无意义。
- **检索精度**要求每个块足够聚焦，使其在向量空间中的位置能够准确反映其语义。一个包含三个不同主题的长块，其向量表示会是三个主题的"平均"，导致对任何单一主题的查询都不够精确。

业界主流的分块策略可以按复杂度分为三个层次：

**固定长度分块**是最简单的方案——按字符数或 token 数切分，相邻块之间保留一定重叠（overlap）。它的优势是实现简单、行为可预测，劣势是完全忽略文档结构，经常在句子中间甚至词语中间切断。适用于结构松散的纯文本（如聊天记录、日志）。

**基于结构的分块**利用文档自身的结构信号进行切分——按段落、按标题层级、按代码函数边界。LlamaIndex 的实践表明，利用 Markdown 标题层级进行递归切分（先按 H1 切，H1 内部再按 H2 切，以此类推）在大多数结构化文档上效果显著优于固定长度切分。这种方法的前提是文档本身有良好的结构标记。

**语义分块**是最精细的方案——通过计算相邻句子的 Embedding 相似度，在语义跳变点进行切分。当连续两个句子的向量相似度低于阈值时，认为发生了主题切换，在此处断开。这种方法能够自适应地识别主题边界，但计算成本较高（需要对每个句子做 Embedding），且阈值的选择需要根据具体语料调优。

### 2.3 分块的工程实践

生产环境中的分块策略通常不是单一方法，而是多种方法的组合。一个常见的工程模式是：

1. 先用结构化方法做粗切（按标题、段落）
2. 对超长块用固定长度 + 重叠做二次切分
3. 对过短块（如单行标题）与相邻块合并
4. 为每个块附加元数据（来源文档、标题路径、页码、块在文档中的位置序号）

块大小的选择是一个需要实验验证的超参数。经验值通常在 256-1024 tokens 之间，但最优值取决于具体场景：FAQ 类短文本适合小块（128-256 tokens），技术文档适合中等块（512-768 tokens），法律合同等需要完整上下文的场景可能需要更大的块（1024-2048 tokens）。

重叠（overlap）的作用是缓解边界处的信息断裂。典型的重叠比例是块大小的 10%-20%。过大的重叠会导致存储膨胀和检索时的重复召回，过小的重叠则无法有效连接跨块信息。

一个值得关注的进阶方向是**父子块索引**（Parent-Child Chunking）：用小块做检索（提高精度），但在送入大模型时返回小块所属的大块（保证上下文完整性）。这种"小块检索、大块生成"的策略在很多场景下能同时兼顾检索精度和生成质量。

---

## 三、Embedding 与向量检索

### 3.1 Embedding 模型

Embedding 模型的任务是将文本映射到高维向量空间，使得语义相近的文本在向量空间中距离相近。这个映射的质量直接决定了向量检索的上限——如果两段语义相关的文本被映射到了向量空间中相距很远的位置，后续的检索算法无论多精巧都无法将它们关联起来。

当前主流的 Embedding 模型可以分为几个梯队：

**通用大模型**如 OpenAI 的 text-embedding-ada-002（1536 维）和 text-embedding-3-large（3072 维），以及国内的 BGE 系列、M3E 等。它们在通用语料上训练，覆盖面广但在特定领域可能不够精确。

**领域微调模型**通过在特定领域语料上继续训练，能够更好地捕捉领域内的语义关系。例如，一个在医疗文献上微调的 Embedding 模型，能够理解"心梗"和"急性心肌梗死"是同一概念，而通用模型可能给出较低的相似度。

模型选择的关键考量包括：向量维度（影响存储成本和检索速度）、最大输入长度（决定了单次编码能处理的文本量）、多语言能力（是否需要处理中英混合文本）、以及推理速度（影响索引构建和在线查询的延迟）。

一个容易被忽略的细节是**查询与文档的不对称性**。用户的查询通常是简短的问题（"如何配置 SSL 证书？"），而文档块是陈述性的段落（"SSL 证书的配置步骤包括..."）。一些模型（如 BGE 系列）通过在查询前添加特定前缀（如 "Represent this sentence for searching relevant passages:"）来处理这种不对称性，在实践中能带来可测量的检索质量提升。

### 3.2 向量数据库

向量数据库是 RAG 系统的存储和检索基础设施。它需要解决的核心问题是：在百万甚至亿级向量中，如何在毫秒级延迟内找到与查询向量最相似的 Top-K 个结果。

精确的最近邻搜索（暴力遍历所有向量计算相似度）在大规模数据上不可行——百万级 1536 维向量的暴力搜索需要数秒。因此生产环境使用近似最近邻（ANN）算法，用少量精度损失换取数量级的速度提升。

主流的 ANN 算法包括：

**HNSW（Hierarchical Navigable Small World）**是目前最广泛使用的算法。它构建一个多层图结构，查询时从顶层开始逐层向下搜索，每层都在局部邻域内贪心地向目标靠近。HNSW 的优势是查询速度快（通常 < 10ms）且召回率高（> 95%），劣势是内存占用大（需要将整个图结构加载到内存）和索引构建时间较长。

**IVF（Inverted File Index）**系列算法先将向量空间聚类为多个 Voronoi 单元，查询时只在最近的几个单元内搜索。IVF 的内存效率优于 HNSW，但查询精度和速度通常略逊。IVF 可以与量化技术（PQ、SQ）结合，进一步压缩存储。

**DiskANN** 是微软提出的磁盘友好型算法，适合数据量超出内存容量的场景。它将图索引存储在 SSD 上，通过精心设计的 I/O 模式实现接近内存索引的查询性能。

美团内部的向量数据库选型实践表明，对于大多数 RAG 场景，Milvus（美团数据库团队提供的定制版本）是推荐选择——它支持 HNSW、IVF、DiskANN 等多种索引算法，最大支持 32768 维向量，单索引可达 10 亿级数据量。对于已有 Elasticsearch 基础设施的团队，ES8 的向量检索功能（基于 HNSW）也是一个可行选项，优势是可以复用现有的全文检索能力，劣势是向量维度上限为 4096 且向量检索性能不如专用向量数据库。

### 3.3 相似度度量

向量检索的"相似度"需要一个数学定义。常用的度量方式包括：

**余弦相似度**（Cosine Similarity）衡量两个向量方向的一致性，忽略长度差异。它是最常用的度量方式，适合大多数文本语义匹配场景。

**内积**（Inner Product / Dot Product）同时考虑方向和长度。当向量已经归一化时，内积等价于余弦相似度。

**欧氏距离**（L2 Distance）衡量向量空间中的绝对距离。在某些需要考虑"语义强度"的场景下有用，但在文本检索中不如余弦相似度常用。

选择哪种度量方式取决于 Embedding 模型的训练目标——大多数现代 Embedding 模型在训练时使用余弦相似度作为优化目标，因此检索时也应使用余弦相似度以保持一致性。

---

## 四、Query 改写、混合检索与 Rerank

如果说文档处理和 Embedding 决定了知识库的"静态质量"，那么 Query 改写、混合检索和 Rerank 则决定了每次查询的"动态质量"。这三个环节共同构成了 RAG 系统的检索优化层。

### 4.1 Query 改写

用户的原始查询往往不是最优的检索输入。它可能过于口语化（"那个东西怎么弄"）、过于简短（缺乏上下文）、包含歧义（"苹果"是水果还是公司）、或者是一个复合问题（"对比 A 和 B 的优缺点并给出推荐"）。Query 改写的目标是将用户意图转化为更适合检索系统理解的形式。

主流的 Query 改写策略包括：

**HyDE（Hypothetical Document Embeddings）**是一种反直觉但有效的方法：先让大模型根据查询"假想"一个可能的答案文档，然后用这个假想文档的 Embedding 去检索。其原理是假想文档与真实答案文档在向量空间中的距离，通常比短查询与答案文档的距离更近。HyDE 在事实性问答场景下效果显著，但在需要精确关键词匹配的场景下可能引入噪声。

**Multi-Query**将一个复杂查询拆分为多个子查询，分别检索后合并结果。例如，"对比 Redis 和 Memcached 的性能和持久化能力"可以拆分为"Redis 性能特点"、"Memcached 性能特点"、"Redis 持久化机制"、"Memcached 持久化支持"四个子查询。这种方法能提高复合问题的召回率，但需要额外的去重和融合逻辑。

**Step-back Prompting**让大模型先将具体问题抽象为更高层次的问题，用抽象问题检索背景知识，再结合背景知识回答原始问题。例如，"为什么我的 Python 3.9 程序在 Ubuntu 20.04 上 import ssl 报错"可以先抽象为"Python SSL 模块的系统依赖关系"。

**Query Expansion**通过同义词扩展、上下位词补充等方式丰富查询的覆盖面。这在关键词检索中尤其有用——用户可能搜索"内存泄漏"，但文档中使用的是"memory leak"。

### 4.2 混合检索

单一检索方式都有盲区：向量检索擅长语义匹配但对精确关键词不敏感，关键词检索（BM25）擅长精确匹配但无法理解语义等价。混合检索（Hybrid Search）将两者结合，取长补短。

典型的混合检索架构是：对同一个查询，同时执行向量检索和 BM25 关键词检索，各自返回 Top-K 候选，然后通过融合算法合并为统一的排序结果。

**RRF（Reciprocal Rank Fusion）**是最常用的融合算法。它的核心思想是：一个文档在多个检索器中排名都靠前，那它大概率是相关的。RRF 的计算公式为 `score(d) = Σ 1/(k + rank_i(d))`，其中 k 是平滑常数（通常取 60），rank_i(d) 是文档 d 在第 i 个检索器中的排名。RRF 的优雅之处在于它只依赖排名而非原始分数，因此可以无缝融合不同量纲的检索结果（向量相似度是 0-1 的浮点数，BM25 分数是无界的正数）。

混合检索的权重分配是一个需要根据场景调优的参数。对于技术文档检索（包含大量专有名词和代码片段），关键词检索的权重应该更高；对于开放式问答（用户用自然语言描述问题），向量检索的权重应该更高。一个常见的起始配置是向量检索权重 0.7、关键词检索权重 0.3，然后根据评估结果调整。

### 4.3 Rerank

Rerank（重排序）是检索链路中投入产出比最高的优化环节。它的作用是对初步召回的候选文档进行精细化排序，将最相关的文档排到最前面。

为什么需要 Rerank？因为向量检索的排序质量有天然上限。向量检索使用的是 Bi-Encoder 架构——查询和文档分别独立编码为向量，然后计算向量间的相似度。这种架构的优势是速度快（文档向量可以预计算），但劣势是查询和文档之间没有交互——模型无法根据具体查询来"理解"文档的哪些部分是相关的。

Rerank 模型使用 Cross-Encoder 架构——将查询和文档拼接在一起作为输入，让模型在 attention 层中充分交互后输出相关性分数。这种架构能够捕捉查询与文档之间的细粒度语义关系（例如，查询中的否定词如何影响与文档的匹配），排序质量显著优于 Bi-Encoder。代价是计算成本高——每个 query-document 对都需要一次完整的模型前向传播，因此只能用于对少量候选（通常 20-50 个）进行重排序，而非全库扫描。

这就形成了 RAG 检索的经典两阶段架构：**粗排（Bi-Encoder 向量检索，从百万级文档中召回 Top-100）→ 精排（Cross-Encoder Rerank，从 Top-100 中选出 Top-5 送入生成模型）**。这种架构在检索质量和计算成本之间取得了良好的平衡。

主流的 Rerank 模型包括 Cohere Rerank、BGE-Reranker、以及基于 BERT/RoBERTa 微调的各类模型。选择时需要考虑延迟要求（Cross-Encoder 的推理延迟通常在 50-200ms，取决于候选数量和模型大小）、语言支持（中文场景需要选择在中文语料上训练过的模型）、以及是否支持长文本输入。

一个实践中的关键决策是 Rerank 的候选数量（即粗排返回多少个文档给 Rerank）。候选太少可能遗漏相关文档（Rerank 无法"创造"新的候选），候选太多则增加延迟和成本。经验值通常在 20-50 之间，具体取决于场景的召回率要求和延迟预算。

---

## 五、评估与工程趋势

### 5.1 RAG 评估体系

RAG 系统的评估比传统 NLP 任务更复杂，因为它涉及多个组件的协同工作。一个端到端的回答质量问题，可能源于检索失败（没找到相关文档）、排序失败（相关文档排名太低被截断）、或生成失败（找到了相关文档但模型没有正确使用）。因此，RAG 评估需要同时覆盖组件级指标和端到端指标。

**RAGAS（Retrieval Augmented Generation Assessment）**是目前最广泛使用的 RAG 评估框架。它定义了一组互补的指标，覆盖检索和生成两个阶段：

**检索阶段指标：**

- **Context Precision**：检索结果中相关文档的排序质量。如果相关文档存在但排名靠后，说明排序策略需要优化（可能需要引入 Rerank 或调整检索权重）。
- **Context Recall**：参考答案中的关键信息有多少能在检索结果中找到。低 Context Recall 意味着知识库覆盖不足或检索策略有盲区（可能需要增加 Top-K、调整分块策略、或引入 Query 改写）。
- **Context Relevance**：检索结果与用户查询的相关程度。低 Context Relevance 意味着检索引入了大量噪声（可能需要收紧相似度阈值或优化 Embedding 模型）。

**生成阶段指标：**

- **Faithfulness（忠实度）**：生成的回答是否忠实于检索到的上下文。这是 RAG 最核心的指标——如果模型无视检索结果自由发挥，RAG 就失去了意义。Faithfulness 的计算方式是将回答拆分为多个原子声明（claims），然后逐一验证每个声明是否能在检索上下文中找到支撑。
- **Answer Relevance**：生成的回答是否切题回答了用户的问题。一个忠实于上下文但答非所问的回答，Faithfulness 高但 Answer Relevance 低。
- **Answer Correctness**：生成的回答与标准答案（ground truth）的一致程度。这个指标需要人工标注的标准答案，成本较高但最直接。

### 5.2 评估的工程实践

构建评估体系的第一步是建立**黄金测试集（Golden Set）**。一个高质量的测试集应该覆盖：不同难度级别（简单事实查询 vs 复杂推理问题）、不同查询类型（精确匹配 vs 语义匹配）、边界情况（知识库中不存在答案的问题、需要跨多个文档综合的问题）。测试集的规模通常在 100-500 条之间，太少不具统计意义，太多维护成本过高。

评估应该是**持续的、自动化的**。每次修改分块策略、更换 Embedding 模型、调整检索参数后，都应该在同一个测试集上运行评估，对比前后指标变化。这种"评估驱动开发"（Evaluation-driven Development）的理念，能够避免"凭感觉优化"导致的负向迭代。

一个实用的评估分层策略是：

1. **离线评估**：在测试集上计算 RAGAS 指标，用于指导系统迭代方向
2. **在线评估**：通过用户反馈（点赞/点踩、追问率、会话长度）间接衡量系统质量
3. **人工抽检**：定期抽样检查系统回答，发现自动指标无法捕捉的质量问题

### 5.3 工程趋势

RAG 技术正在从"能用"走向"好用"，几个值得关注的演进方向：

**Agentic RAG**将 RAG 从被动的"一次检索一次生成"升级为主动的多步推理。Agent 可以根据初次检索结果判断信息是否充分，如果不充分则自动改写查询、切换检索策略、或从不同数据源补充信息。这种自适应检索能力显著提升了复杂问题的回答质量，但也引入了延迟和成本的不确定性。

**Graph RAG**将知识图谱与向量检索结合。传统向量检索只能找到与查询"语义相近"的文档块，但无法进行多跳推理（"A 的老板是 B，B 所在的部门负责什么业务"）。Graph RAG 通过在文档块之间建立实体关系图，支持沿关系链进行推理式检索。

**长上下文模型对 RAG 的影响**是一个持续讨论的话题。随着模型上下文窗口从 4K 扩展到 128K 甚至 1M，是否还需要 RAG？答案是肯定的，但原因在变化——长上下文模型减少了对精确检索的依赖（可以放入更多候选文档让模型自己筛选），但 RAG 在成本控制（不需要每次都处理百万 token）、知识更新（改文档比重新加载上下文快）、可追溯性（能指出答案来自哪个文档的哪个段落）方面仍然不可替代。

**多模态 RAG**将检索对象从纯文本扩展到图片、表格、代码、音视频。这要求 Embedding 模型能够处理多种模态的输入，向量数据库能够存储和检索多模态向量，生成模型能够理解和引用多模态证据。

---

## 六、设计 Checklist

以下是构建生产级 RAG 系统时需要逐一确认的关键决策点：

**文档处理层：**
- 文档格式是否统一？解析器是否覆盖所有格式（PDF/Word/HTML/Markdown）？
- 分块策略是否经过实验验证？chunk size 和 overlap 是否针对业务场景调优？
- 是否保留了文档元数据（标题、来源、时间戳、层级关系）？

**Embedding 与索引层：**
- Embedding 模型是否与业务语言匹配（中文/英文/混合）？
- 向量维度与检索性能的平衡点是否确认？
- 索引类型（HNSW/IVF/DiskANN）是否根据数据规模和延迟要求选定？
- 是否建立了增量更新机制，避免全量重建？

**检索与排序层：**
- 是否采用混合检索（向量 + 关键词）？RRF 权重是否调优？
- 是否引入 Rerank 模型？Rerank 的延迟开销是否可接受？
- Top-K 和相似度阈值是否经过 A/B 测试？
- Query 改写策略是否覆盖了模糊查询、多意图查询、对话式查询？

**生成与输出层：**
- Prompt 模板是否明确指示模型基于检索结果回答？
- 是否有 Faithfulness 检查机制防止幻觉？
- 是否支持引用溯源（标注答案来自哪个文档段落）？
- 对于检索结果为空的情况，是否有优雅的降级策略？

**评估与监控层：**
- 是否建立了包含 golden set 的评测数据集？
- 评估指标是否覆盖检索质量（Recall/MRR）和生成质量（Faithfulness/Relevance）？
- 是否有线上监控看板追踪检索命中率、用户满意度、延迟分布？
- 是否建立了 bad case 收集和迭代闭环？

**工程与运维层：**
- 向量数据库的容量规划是否考虑了未来 6-12 个月的增长？
- 是否有灾备和数据一致性保障？
- 成本模型是否清晰（Embedding 调用费用 + 向量存储费用 + Rerank 费用 + LLM 生成费用）？
- 是否预留了 A/B 测试基础设施，支持快速验证优化效果？
