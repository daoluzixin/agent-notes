# 让 Agent 从 0 到 1 自主搭建系统——文档约束 + 自反馈链路

> 主题：Agent Harness Engineering 实践方法论
> 核心论点：Vibe Coding 的关键不在模型能力，在于 Harness 环境的搭建

本文介绍一套让 Agent 在空仓上从零搭建可运行系统的完整方法。核心思路分为两个维度：知识约束（让 Agent 不跑偏）和自反馈闭环（让 Agent 不盲写）。最终产出是一个可被团队复制的工程流程，而非一次性的个人经验。

这里并不是想证明"Agent 会写代码"，而是想说明一件更重要的事：如果想让 Vibe Coding 真正从 0 到 1 跑起来，不能只靠模型能力，必须先把 Harness 环境搭起来。

这个观点与 OpenAI 的 Harness Engineering 主旨一致：

> 工程师的角色会从"直接写代码"转向"设计环境，让 Agent 自主完成任务"。

UI、日志、指标、测试结果这些运行态信号，必须能被 Agent 直接读取和利用。计划与执行日志，不能只存在聊天记录里。

在这次实践中，Agent 基于知识约束和自反馈链路长时运行约两小时。从列出 MVP 计划开始，Agent 写完代码、发布到 SCM 仓库、上线部署、发 API 测试、查看测试结果、拉日志分析——整个过程全程不漂移。

---

## 第一部分：文档约束与计划设计——如何把一个空仓搭成可执行系统

### 1. 先搭"知识控制面"，而不是先写代码

项目一开始，最先做的不是 handler、service、repo，而是把 Agent 的知识约束环境搭出来。目的是让任何一个 Agent 进来都知道：项目架构、背景、核心功能是什么，项目的约束是什么，当前该做什么。

初版目录结构如下：

```
AGENTS.md              ← 只做导引，不做百科全书
docs/agents/
  ├── 目元信息.md         ← 项目背景/阶段/核心概念
  ├── 项目初始技术方案.md    ← 项目核心架构
  ├── 工程约束与协作规范.md   ← 命名风格/代码规范
  └── 自反馈工作流.md       ← 自反馈闭环流程
docs/exec-plans/
  ├── active/            ← 当前正在执行的计划
  ├── completed/         ← 已完成的计划
  └── activeLog/         ← 执行日志
```

这套结构是随着项目成长而不断迭代的。随着工程量增加，结构可能逐渐演变为更深的层级——核心是明确定义好规范，选择适合自己的工程知识约束：

```
AGENTS.md
docs/
  agents/
    ├── 目元信息.md
    ├── 核心架构.md
    ├── 工程约束.md
    └── 自反馈工作流.md
  design-docs/          ← 长期设计文档
  exec-plans/
    ├── active/          ← 正在执行的计划
    ├── completed/       ← 已完成的计划
    └── activeLog/       ← 执行日志
  product-spec/         ← 产品功能文档
  references/           ← 外部参考
```

### 2. AGENTS.md 只做地图，不做百科全书

AGENTS.md 只做文档导航，不承载大段项目说明和规则细节。它定义了首读顺序——拿到任何任务时，如果对本项目还不熟悉，先按下面顺序阅读：`docs/agents/目元信息.md` → `docs/agents/核心架构.md` → `docs/agents/工程约束.md`。

按场景查文档——这是整个文档体系里最关键的设计之一。没有把所有规则都塞进 AGENTS.md，而是把它严格收敛成"导引文件"：首读顺序 + 按场景查文档。

为什么这么做？因为一个超长的 AGENTS.md 有三个致命问题：信息太多导致真正重要的内容被淹没；一旦过时会灾难性地误导 Agent，无法判断哪些内容是当前有效事实、哪些只是历史残留；AGENTS.md / Claude.md 每次对话会全量加载到 prompt 中，造成上下文过载。

所以 AGENTS.md 只负责"导航"。真正的知识，拆到 `docs/agents/` 里维护。

### 3. 长期知识必须拆开，不能混成一锅粥

长期知识最终拆成了几类文档，每份文档只回答一个问题：

| 文档 | 回答什么问题 |
|------|------------|
| 目元信息.md | 这个仓是做什么、处于哪个阶段、PM 是谁、dev 环境是什么 |
| 项目初始技术方案.md | 项目要解决什么问题，核心架构决策是什么 |
| 核心架构.md | 当前代码落在哪一层、目录边界、模块职责 |
| 工程约束与协作规范.md | 哪些事情能做，哪些事情不能做 |
| 自反馈工作流.md | 代码写完之后怎么验证 |

这套结构分别解答了四个问题：Agent 进仓先读什么去理解事实；哪些边界能动、哪些边界不能动；一个功能按什么顺序做、做到什么算完；长任务、多会话、多 Agent 接力时状态怎么不断片。

这个设计看起来像文档管理，但本质上是在做"知识隔离"。Agent 最怕的不是信息少，而是信息混在一起、口径互冲突、不知道应该信谁。

### 4. 把模糊需求讨论成可执行计划

在写每一个需求的时候，停下来问自己：

> 你是否懂了这个需求的完整链路？

如果懂了，有两个选择：自己拆分，或者让需求分配者帮你拆。如果不清楚，Agent 就会漂移，最终写出的代码一定是残缺的。

把需求文档给 Agent，不断地和 Agent 沟通。和 Agent 的对话重点，不是"帮我把代码写出来"，而是先把模糊需求打磨成一份没有歧义的执行计划。这一步的核心不是"快速出版"，而是让每个会话的实现和验收定义在动手前有共识，让 Agent 对于本次需求的执行无歧义。

### 5. 计划不是备忘录，而是一等工件

当文档约束和工程约束定义完之后，才开始为功能写执行计划。每个功能单独拉成计划，放进 `docs/exec-plans/active/` 目录下面——放的是当前未完成的活跃需求，让 Agent 每次都能从这个目录下找到未完成的计划。直到任务完成后将计划文件移到 `docs/exec-plans/completed/`。

计划至少会先定这几项：功能边界、对外契约/接口变化、关联依赖、验收标准、自反馈验证方式。

Agent 在开始写代码之前，就已经知道：这次只做什么不做什么、落到哪一层、成功是什么样失败怎么判断。

执行计划示例片段：

```
DeductCredits 最终执行计划

状态
当前状态：active
可选值：待执行 / 执行中 / 完成

执行记录要求
执行本计划时，每完成一步关键操作，都必须更新 activeLog.md
执行记录至少包括：
- 当前步骤做了什么
- 是否涉及代码改动
```

### 6. activeLog 是计划状态机，也是外置记忆

只靠聊天上下文，长任务一定会失忆。所以补了三层状态系统：**active**（当前正在执行的计划）、**completed**（已经验收完成的计划）、**activeLog**（每一步关键动作和结果记录）。

你是否会有这样的担心？上一轮的执行到底是什么？需求真的完了吗？联系上下文记录具体干了什么？说代码？太多了，让 Agent 实时记录本次执行中关键的核心操作就行了——关注执行流，而不是代码。

以下是 Agent 执行过程中的记录片段：

```
activeTestLog

用途
本文件用于记录本轮四接口线上联动与业务大范围验证的核心测试日志。
所有 case 级记录、请求响应、回查结果、Log 检索、日志复验结果、Bug 记录，
都统一追加到本文件。
若测试中发现接口 bug 或业务 bug，必须先记录到本文件，再进入后续处理。

记录规则
- 一条 case 至少记录一次执行结果
- 若暴露 bug，必须补充：bug 分类
```

执行日志覆盖的内容包括：每步操作践行了什么、是否涉及代码改动，commit/push 信息，API 请求/响应结果，测试结果，是否需要续改代码重试。

这件事特别重要，因为它把"我确实做到了"从一个模糊感觉，变成了仓库里的明确事实。

---

**Part 1 最小启动动作清单：** 建 AGENTS.md 只做导引 → 建 `docs/agents/目元信息.md` → 建 `docs/agents/项目核心架构.md` → 建 `docs/agents/工程约束与协作规范.md` → 如果项目已有方案或背景补充 `项目初始技术方案.md` → 为第一个要做的功能写一份 exec-plan。

把计划、状态、执行日志的机制建好。到这里，项目就不再是"一个空仓 + 一个模糊需求"，而是已经有了最小可执行控制面。

---

## 第二部分：自反馈工作流与测试闭环——代码写完之后如何让 Agent 自己验证

### 1. 代码能编译，不等于功能真的生效

这是第二个必须建立的系统。如果只有文档约束，没有自反馈闭环，Agent 最终还是会退化成代码打字机。工程瓶颈很快会从"写代码"变成"验证结果"。所以 UI、日志、指标、测试结果这些运行态信息，必须能直接被 Agent 读取。

落到实践里，这件事可以概括成一句话：

> **Harness = 文档约束 + 自反馈闭环**

前者负责不跑偏，后者负责不盲写。

### 2. 自反馈闭环的定义

最后沉淀下来的固定闭环是：

**代码 → commit/push → SCM 构建 → 测试环境部署 → API Test → 查日志 → 不通过就修 → 重新部署**

这条链路最终被写进了 `docs/agents/自反馈工作流.md`，而不是只存在脑子里。它的意义是：不允许停在"代码看起来对"，必须走到"环境里真的跑过，并且有日志证据"。

自反馈工作流的适用场景是：当任务涉及"代码开发 → 发布测试版本 → 部署测试环境 → 发 API 请求 → 查日志 → 修代码复验"这一整套完整 runtime 验证的需求时阅读。特别适合异步链路、回调链路、observer 埋点、错误码透传、产物字段、字段全链路透传等场景。

核心目标是让 Agent 不停留在"代码看起来对"，而是走到"测试环境已验证、日志已证明代码确实生效"。

### 3. 执行元信息作为唯一事实源

为了让 Agent 针对不同的需求对应着不同的环境，又加了一份 `docs/exec-plans/active/当前活跃需求执行说明.md`。这里面钉死的不是业务逻辑，而是这轮执行所需的环境事实：当前代码分支、执行顺序、推荐使用的 skill、测试 IDL 分支和版本、测试 PSM、控制面 Env、集群、阻塞处理规则。

示例片段：

```
状态
当前状态：active

适用范围
feat/init 下"四接口线上联动与业务大范围验证执行计划"

目的
本文档用于给本次四接口线上大范围测试提供唯一执行顺序，
统一测试参数和统一记录方式。当单个测试计划中的默认参数、
历史参数或旧说明与本文档不一致时，以本文档为准。

本文档是执行说明，不替代具体 case 清单、业务语义说明和修复细节。
```

这一层很关键，因为它把"运行时元信息"从聊天里的临时说明，升级成了能统一读取的事实源。

### 4. 闭环的实际运行效果

这次不是抽象地说"可以自反馈"，而是确实跑过了完整闭环。MVP 所有的接口最终都走过了这条链路：

**本地 go build/test → SCM 测试构建 → ENV 部署 → API Test 发真实请求 → Argos/日志检索复验 → 失败则继续改代码、重新部署、再测**

真正能说明问题的，是中间两次典型的运行态纠偏。

---

**例子 1：API Test 一开始打不通——不是代码错，而是 IDL/测试口径不一致**

一开始并不是代码直接失败，而是 API Test 平台使用的 schema 和当前服务实际运行的 IDL 口径不一致。如果没有自反馈闭环，这类问题很容易被误判成服务没部署成功、handler 没命中、或者代码没生效。

但因为把"测试参数、IDL 分支、IDL 版本、PSM、环境"都前置写进了执行说明，所以这件事最后能被定位成测试入口口径问题，而不是业务代码本身的问题。

---

**例子 2：线上日志打出 Unknown column 'AIOId'——倒逼 repo 层字段对齐真实 DDL**

这次最有代表性的运行态反馈，是查询链路第一次真正打到新 pod 后，日志里直接暴露了：

```
Unknown column 'AIOId' in 'where clause'
```

这个问题不可能靠"看代码感觉"发现。它必须经过部署到真实环境 → 发真实请求 → 命中新 pod → 定点查日志这四步才能暴露。最后才知道不是 IDL 问题也不是 handler 问题，而是 repo 层字段名和线上真实 DDL 没对齐。

这就是自反馈闭环真正的价值：它不是帮你"更快确认自己是对的"，而是帮你更快发现自己到底错在哪。

### 5. 执行日志对自反馈同样重要

很多人以为有了部署和 API Test 就够了，其实不够。如果没有执行日志，长任务跑一段时间之后，Agent 和人都会忘：这轮到底改了什么、测的是哪组参数、部署到了哪个版本、返回体是什么、上一轮失败的结论是什么。

所以要求每一步关键动作都写进 activeLog.md 和测试日志。这会带来两个直接收益：我睡觉的时候 Agent 还能继续工作；我醒来之后只要扫日志和测试结果，就能判断当前这一轮是否真的落地。

---

**Part 2 最小可复制模板：** 先写一份项目级 `自反馈工作流.md` → 明确构建入口、部署入口、测试入口、日志入口 → 为当前批次任务再写一份"执行说明"把运行时元信息钉死 → 强制要求每一步关键动作写执行日志 → 不拿到运行态证据就不允许把计划移到 completed/。

---

## 第三部分：对抗测试——需求跑通不等于接口健壮

### 1. 接口能跑通，核心业务功能和健壮性就真的稳了吗？

对于一个需求，经过了自反馈验证不代表接口是稳定的。线上实际业务数据还没打过来，接口通不代表业务通。

所以需要大量覆盖测试。测试集不是人工出——开另一个 Agent 让它针对业务接口出对抗测试数据集，并列出对抗测试计划。然后让需求 Session Agent 拿着测试计划和测试覆盖数据集再去跑自反馈，遇到 bug 就修复重跑，直到所有数据集完成。

### 2. 对抗测试的执行结构

对抗测试同样遵循分层记录原则。Agent 生成的业务测试计划示例：

```
四接口线上联动与业务大范围验证执行计划

状态
当前状态：执行中
可选值：待执行 / 执行中 / 完成

执行记录要求
执行本计划时，记录拆成两层：
- 阶段级/里程碑级进展，记录到 docs/exec-plans/activeLog/activeLog.md
- case 级核心测试日志、请求响应、回查结果、bug 记录，记录到
  docs/exec-plans/activeLog/activeTestLog.md
```

测试过程中 Agent 产生的操作日志结构：

```
activeTestLog

用途
本文件用于记录本轮四接口线上联动与业务大范围验证的核心测试日志。
所有 case 级记录、请求响应、账户回查、流水回查、logid、日志复验结果、
bug 记录，都统一追加到本文件。
若测试中发现接口 bug 或业务 bug，必须先记录到本文件，再进入后续处理。

记录规则
- 一条 case 至少记录一次执行结果
- 若 case 暴露 bug，必须补充：bug 分类
```

这套对抗测试机制的关键在于：测试集由独立 Agent 生成（避免实现者自身的盲区），测试执行仍然走自反馈闭环（而非手工验证），所有结果都有日志证据（而非"我跑过了"的口头承诺）。

---

## 第四部分：把约束从"文档"升级成"基础设施"

前面三部分讲的都是"写什么文档"。但文档有一个天然弱点：它只是建议，不是强制。Agent 可以读到文档，也可以选择性忽略。如果你希望约束真正生效，必须把它从"人读的文档"升级成"机器执行的基础设施"。

### 1. Linter 脚本——把品味变成代码

工程约束文档里写了"Controller 不得直接调用 DAO"，但 Agent 写代码时可能忘了。解决方案是写一个 linter 脚本让它自动检查。

具体做法是在仓库里维护一组 shell 脚本，覆盖两类检查：架构约束检查（分层依赖方向、命名规范、模式一致性）和文档完整性检查（必需文档是否存在、SQL 与实体是否对齐、文档引用是否有效）。这些脚本不复杂，大多是 grep + 正则 + 文件存在性判断，但它们的价值在于三点。

第一，把隐性品味变成显性规则。"Controller 不能调 DAO"不再是一句话，而是一条会报 FAIL 的检查项。第二，Agent 每次提交前必须跑通。把 `bash scripts/lint-all.sh` 写进自反馈闭环的第一步，0 FAIL 才允许 push。第三，检查项本身也是文档。新 Agent 进来跑一遍 lint，看到哪些检查项，就知道这个项目在乎什么。

举个例子：架构检查脚本里有一条规则会扫描所有 Controller 文件，检查是否存在直接 import DAO 层的语句。如果有，直接报 FAIL 并告诉 Agent 应该通过 Service 层调用。这条规则写在文档里 Agent 可能忘，但写成脚本后它每次都会被执行。

Linter 脚本的本质是：把你对代码的品味，从"我希望你这样做"变成"你必须这样做，否则过不了"。

### 2. 强制规则的自动注入——让约束不依赖 Agent 的"自觉"

文档约束还有一个问题：Agent 需要主动去读。如果 Agent 没读到工程约束文档就开始写代码，约束就形同虚设。

解决方案是利用 Agent 工具的规则自动加载机制。以 CatDesk 为例，`.catpaw/rules/` 目录下的规则文件会在每次对话开始时自动注入到 Agent 的上下文中。这意味着可以把最关键的约束写成规则文件，Agent 不需要主动去读，它们会自动生效。

实践中把规则分成三类文件：项目约束规则（FBI 函数模式、团队隔离、软删除、凌晨静默窗口——这些是"不能违反的红线"），文档维护规则（改了代码就检查是否需要更新 docs/——这是"容易忘但很重要的习惯"），执行计划纪律（新功能先写计划、执行日志实时更新、不拿到运行态证据不移到 completed/——这是"流程不能跳步"）。

这三类规则加起来不超过 30 行，但覆盖了 Agent 最容易犯的三类错误：违反架构约束、忘记同步文档、跳过验证流程。

规则自动注入 + Linter 脚本，构成了约束的两道防线：前者在 Agent 开始工作时就告诉它"什么不能做"，后者在 Agent 提交代码时验证它"是否真的没做"。

### 3. 上游消费者会反向驱动 API 设计

这是实践中发现的一个意外收获，但回头看其实是必然的。当你的服务不只是被前端页面调用，还被另一个 Agent（比如定时任务、数据分析流水线）作为上游消费者调用时，API 的设计会被反向推动。因为 Agent 消费者对接口的要求和人类消费者不同：它需要批量查询、需要幂等写入、需要明确的错误码而不是模糊的提示语。

启示是：在搭建 Harness 时，不要只考虑"Agent 怎么写代码"，还要考虑"Agent 怎么消费你的服务"。如果你的系统会被其他 Agent 或自动化流水线调用，那么这些消费者的需求应该被写进文档约束里，成为 API 设计的一部分。具体来说，建议在架构文档中明确标注上下游关系：谁在调用你的服务、调用频率是什么、数据流向是怎样的。

### 4. 文档结构不必照搬，但原则要一致

前面给出的目录结构是 `docs/agents/` 下放知识文档。但在实际落地另一个项目时，发现对于中小型项目，扁平化的 `docs/` 目录反而更好——文件不多的时候，多一层目录只是多一次跳转，没有信息隔离的收益。

真正重要的不是目录结构长什么样，而是四个原则：每份文档只回答一个问题，不要把架构、约束、部署混在一个文件里；AGENTS.md 永远只做导航，不管 docs/ 下面是扁平的还是嵌套的，入口文件的职责不变；约束要有自动化守卫，文档告诉 Agent 规则，linter 脚本验证 Agent 是否遵守了规则；关键规则要自动注入，不依赖 Agent 主动去读，而是让工具在每次对话开始时自动加载。

选择适合你项目规模的结构，但不要在原则上妥协。

---

**Part 4 最小动作清单：** 在 `scripts/` 下写一个 lint 脚本至少覆盖分层依赖方向、必需文档存在性、命名规范 → 把 `bash scripts/lint-all.sh` 写进自反馈工作流的 Step 1，0 FAIL 才允许提交 → 在 `.catpaw/rules/` 下创建 2-3 个规则文件覆盖项目约束、文档维护、执行计划纪律 → 在架构文档中标注上下游消费者关系。

到这一步，你的 Harness 就不再只是"一堆文档"，而是一个有自动化守卫、有规则注入、有消费者感知的完整工程环境。

---

## 第五部分：拥有文档约束和自反馈链路后的实际操作流程

拥有了上述体系之后，实际的 Vibe Coding 操作流程就变成了一个可重复、可团队复制的闭环：

1. **搭建知识控制面**（AGENTS.md + docs/agents/ 系列文档）
2. **把需求打磨成无歧义的执行计划**（exec-plans/active/）
3. **Agent 按计划写代码**，每步记录 activeLog
4. **自反馈闭环验证**：commit → 构建 → 部署 → API Test → 查日志
5. **对抗测试**：另一个 Agent 出测试数据集，需求 Agent 跑完全部 case
6. **验收通过**后，计划移到 completed/

这套方法的核心不是让 Agent 更聪明，而是让 Agent 在一个结构化的环境里，不漂移、不盲写、可验证地完成任务。
