# 记忆系统方法论——按需召回的文件系统级状态服务

模型能在同一个会话里利用前文，是因为平台持续提供了当前上下文。进入一个独立会话后，没有被重新注入的信息就不会自然出现。要让协作跨越会话继续下去，重要状态必须离开聊天窗口，进入一个能够被保存、检索、修正和迁移的外部载体。

但持久化只解决“信息不会消失”，没有解决“信息怎样回来”。如果系统不知道什么时候应该读取、当前任务需要哪一部分、读到什么程度应该停止，那么磁盘上的历史只是归档，不是可供 Agent 稳定消费的记忆。

因此，一套完整的记忆系统需要同时具备四种能力：把协作状态外置到磁盘，根据任务语义判断是否需要历史，按主题逐步召回最小相关内容，并持续维护长期记忆的有效性。

```text
记忆系统 = 外置状态 + 路由门禁 + 渐进召回 + 生命周期管理
```

这四部分是一条因果链。外置状态让记忆拥有稳定事实源；路由门禁阻止无关历史进入当前任务；渐进召回控制上下文中的信息量；生命周期管理决定什么可以成为长期认知，以及失效内容如何退出。缺少任何一环，系统都会在长期运行中退化：只有存储无法形成召回，只有召回容易造成上下文污染，只有不断写入则会让记忆逐渐失真。

对应的公开实现位于 [`Fyrgo8/memory-system-skill`](https://github.com/Fyrgo8/memory-system-skill)。仓库只包含可安装的 Skill、确定性脚本和使用说明，不包含任何真实用户记忆。

---

## 一、跨会话记忆首先是一套外置状态

个人单机记忆系统最重要的要求不是吞吐，而是透明和可接管。记忆中的每项事实都应该能被人直接查看，Agent 判断错误时可以手工修正，发生冲突时能追溯来源，迁移时也不依赖某个客户端提供的黑盒能力。

基于这些要求，系统采用纯 Markdown 作为持久化介质：

```text
memory-system-data/
├── MEMORY.md
├── daily/
│   └── YYYY-MM-DD.md
└── reference/
    └── work-memory/
```

这个结构不需要数据库和网络服务。文件可以用普通编辑器打开，可以由全文搜索工具扫描，可以通过 diff 检查变化，也可以整体复制到其他环境。模型只是这些文件的消费者之一，文件系统本身才是跨会话事实源。一个架构决策如果只存在于聊天中，就属于易失状态；只有落到可持续访问的外部载体，后续任务才有机会重新获得它。

纯文本没有天然的语义索引，检索需要依靠主题词、项目名、Issue 编号、日期和全文搜索发现候选，再由模型判断候选是否真正相关。这个取舍与个人单机场景匹配：系统把确定性存储保持得足够简单，把复杂的语义判断留在调用阶段，而不是为尚未出现的规模问题提前引入额外基础设施。

这里需要区分“磁盘搜索”和“上下文注入”。脚本可以扫描整个数据目录，但扫描到的文件不必全部进入模型。真正消耗上下文预算的是返回给模型的候选片段和最终读取内容，而不是文件系统内部完成了多少次匹配。只要注入边界受控，纯文本检索就可以同时保留完整历史与有限上下文。

---

## 二、数据分层依据生命周期，而不是重要程度

`MEMORY.md` 保存长期仍然成立，并且会持续影响后续协作的信息。稳定身份、沟通习惯、长期技术方向、跨项目偏好、已经确认的工作方式，以及指向详细历史的主题索引，都属于这一层。它回答的问题是：一个新的相关任务，需要长期知道哪些事实。

一项信息能否进入长期层，需要连续经过三个判断。首先看时效性，经过一段时间后它是否仍然成立；然后看变化频率，它是稳定事实还是会随任务推进不断变化的状态；最后看跨会话价值，一个新的相关会话是否真的需要知道它。

这三个判断需要结合证据类型。用户明确声明“以后下载模型优先 ModelScope”，它本身就是长期偏好的直接证据；某次任务恰好使用了 ModelScope，只能说明当时的工具选择，不能自动上升为稳定习惯。显式声明可以快速形成长期记忆，从单次行为推断出的规律则需要更多观察。

`daily/YYYY-MM-DD.md` 保存某次协作具体发生了什么，包括任务目标、执行进度、关键判断、方案取舍、踩坑过程和待 follow-up。daily 是情节记忆，也是后续继续项目、复盘决策和形成长期认知时的原始证据。

daily 的写作目标不是复述聊天，而是让未来的相关任务能够恢复现场。一条有效纪要至少需要回答三件事：本轮完成了什么，形成了哪些关键结论或设计决策，还有哪些事情没有结束。纪要长度不能作为写入标准。一篇很长的通用解释可能没有跨会话价值；一个只涉及两行代码、却决定了后续架构方向的结论，则需要进入 daily。真正的判断标准是：未来继续这个主题时，缺少这条记录是否会造成重复劳动、决策断裂或错误重做。

daily 按日期落盘，是因为日期适合稳定追加和追溯；读取时则按主题组织，因为时间接近不等于语义相关。昨天的学习记录未必能帮助今天继续某个长期项目，半个月前围绕该项目形成的关键决策反而可能是当前任务真正需要的上下文。时间负责保存，语义负责召回。

`reference/work-memory/` 保存低频完整历史和迁移快照。它的信息最完整，访问优先级也最低。一次正常召回应先从 `MEMORY.md` 获取相关长期摘要，再从 daily 中读取与主题直接相关的片段。只有这两层无法支撑当前任务时，才进入历史快照查找对应主题。

```text
相关长期摘要
    ↓ 信息不足
相关 daily 片段
    ↓ 仍然不足
历史快照中的对应主题
```

每一层都必须有停止条件。信息已经足够，就结束读取。完整历史得到保留，不代表完整历史需要同时进入上下文。

知识还需要在不同生命周期之间流动。新信息默认停留在 daily；经过后续协作验证，确认它具有长期时效性、低变化频率和跨会话价值后，再晋升到 `MEMORY.md`。长期记忆也需要持续维护。身份会变化，项目会结束，工具会替换，同一事实可能出现更新版本。每次维护都应在新增、更新、删除、忽略中做出明确选择，而不是通过不断追加让多个冲突版本长期并存。

这也是记忆系统与归档系统的边界。归档追求完整保留过去，记忆追求让未来任务获得仍然有效的过去。允许晋升，也允许纠错和遗忘，`MEMORY.md` 才能保持高信噪比。

---

## 三、路由门禁决定记忆何时进入上下文

新会话只说明当前上下文是新的，不能说明当前任务依赖历史。系统在用户提出具体任务之前保持零记忆 I/O：不检查 `MEMORY.md`，不扫描 daily，也不为了“可能有用”加载用户画像。记忆读取是一项由任务语义触发的服务调用，不是会话生命周期中的固定动作。

例如，“解释一个通用技术概念”是自包含问题，用户历史不会改变核心原理，读取项目和偏好没有必要。“继续上次那个还没完成的项目”表达了对历史的依赖，却没有给出足以定位记忆的主题；系统应该先确认具体项目或任务，再决定读取哪些历史。

`read-context` 的隐式触发必须同时满足四个条件：当前任务明确指向用户历史、长期偏好、项目延续状态或既往主题决策；当前消息与工作区信息不足以完成任务；缺少历史会实质影响结果的正确性、一致性或连续性；系统能够提炼出一个具体主题或 2—6 个检索词。

这四个条件使用 AND 连接，因为它们共同建立读取权限。任务与历史有关，但用户已经在当前消息中贴全资料，历史不是必要输入；当前信息不完整，但问题与用户历史无关，也不能通过读取记忆补齐；系统无法说清需要寻找什么时，则应该先澄清主题，而不是用空查询扫描整个数据目录。

排除条件使用 OR。常识问答、通用概念解释、翻译润色、一次性浅问浅答、自包含任务，以及与用户身份或历史项目无关的普通操作，任意一项成立就不读取记忆。

触发和排除采用不对称结构，是因为误触发与漏触发的代价不同。漏触发可以通过用户补充“接着上次”恢复；误触发会让无关历史直接进入推理过程，可能从一开始就改变回答方向。因此，读取门禁优先保证精度，而不是追求每段历史都有机会被召回。

用户显式调用 `$memory-system`，或明确要求“继续上次”“回忆之前的决定”，可以直接建立调用意图，但仍然需要限定主题。显式调用授权的是查询某段历史，不是把全部过去装入上下文。

检索从当前任务提炼主题，再生成项目名、对象名、Issue 编号或核心概念等关键词。日期范围只在已经明确主题时用于缩小候选，不负责代替语义判断。这条规则使数据的时间组织与调用的主题组织各司其职：日期保证写入和追溯稳定，主题保证召回与当前任务相关。

---

## 四、四种操作隔离读取、写入、晋升与维护权限

记忆系统包含读取、记录、晋升和维护四类行为。它们处理的对象不同，对历史数据的权限也不同，因此必须先确定操作，再进入相应执行路径。

| 操作 | 解决的问题 | 历史读取权限 |
|---|---|---|
| `read-context` | 当前任务缺少历史上下文 | 按主题最小读取 |
| `append-daily` | 当前交付需要为未来保留现场 | 不允许 Search/Read |
| `promote` | 已验证信息需要进入长期记忆 | 只读待判断的相关记录 |
| `maintain` | 记忆需要更新、纠错、删除或整理 | 只读需要修改的目标段落 |

`read-context` 的目标不是展示搜索结果，而是为当前任务补齐缺失状态。它接收任务目标、主题、检索词和可选日期范围，经过候选发现和精确读取后，形成一份最小上下文包。上下文包记录实际读取来源、相关事实、无法消解的冲突和停止原因。对用户的最终回答只使用完成任务所需的信息，不倾倒记忆原文；系统内部则保留足够证据，能够说明某项历史判断来自哪里。

`append-daily` 是一条独立的只写路径。完成有未来延续价值的交付后，当前会话已经包含本轮需要保存的事实，系统直接依据当前上下文生成纪要，不调用 Search，也不读取 `MEMORY.md` 或既有 daily。

这种权限隔离保证“交付后写”不会暗中变成一次历史召回。写入所需的信息来自当前会话，读取历史必须重新满足 `read-context` 的路由契约。两个操作即使属于同一个 Skill，也不能互相继承权限。纯确认、闲聊、常识问答和一次性任务不进入 daily；项目里程碑、后续会依赖的决策、需要避免重复踩坑的事实，以及用户明确要求记录的内容才触发写入。

`promote` 负责把经过验证的信息从情节层提升为稳定认知。晋升判断关注证据，而不是出现次数。用户明确声明的长期偏好具有直接证据，可以快速晋升；从单次行为推断出的偏好需要多次观察；项目当前进度天然属于动态状态，即使频繁出现，也不应被写成长期身份。

`maintain` 负责让长期记忆与现实保持一致。用户当前陈述与记忆冲突时，以当前陈述为准并修正目标段落；项目结束后，相关状态应退出当前长期认知；重复条目需要合并，而不是通过追加形成多个互相竞争的版本。维护操作只读取需要修改的目标范围，不把局部更新扩大成全库扫描。

失败处理也服从操作边界。主题为空或范围过宽时，系统只询问一个最小澄清问题；搜索无命中时返回 `no_match`，并基于当前上下文继续能够完成的部分；候选过多时收紧关键词或日期范围；历史不足时明确承认缺少记忆。检索失败不会自动扩大读取权限，写入失败也不会切换成另一种操作。

记忆与现实发生冲突时，系统按证据的新鲜度和直接性合并：

```text
当前用户消息
    > 当前工作区事实
    > 较新的 daily
    > MEMORY.md
    > 历史快照
```

这个顺序不表示 daily 天然比 `MEMORY.md` 更可信，而是表示较新的直接证据优先。记忆用于补充现实，不能反过来用旧状态否定用户刚刚提供的新事实。无法判断的冲突需要显式保留，并交给后续维护处理。

---

## 五、Skill 以服务契约组织渐进加载与执行边界

记忆系统的控制面分成四层：

```text
L0  AGENTS.md          全局路由门禁
L1  Skill description  服务发现
L2  SKILL.md           执行契约
L3  scripts/           确定性数据操作

数据面
├── MEMORY.md
├── daily/
└── reference/work-memory/
```

`AGENTS.md` 是高优先级常驻入口，只保存新会话零记忆 I/O、隐式触发 AND、排除 OR，以及 `append-daily` 不授予读取权限等稳定门禁。它不承载完整检索步骤和数据格式，因为所有会话都会付出这部分上下文成本。

description 负责让 Agent 在多个 Skill 中识别记忆服务。它只需要说清做什么、何时使用、何时不用和输出什么，信息量控制在约 100 个字符。路由命中后才加载完整 `SKILL.md`，输入字段、缺失行为、输出契约、执行步骤、失败语义和版本规则都位于这一层。搜索和写入等不依赖模型判断的操作放进 scripts，在执行到对应步骤时调用。

```text
L1  name + description     服务发现
L2  SKILL.md               完整执行契约
L3  scripts / references   按需执行与参考
```

Skill 自身的渐进加载解决“操作说明是否进入上下文”，记忆内容的渐进召回则解决“哪些历史进入上下文”。两者需要分开理解。Skill 被触发，只表示当前任务需要记忆服务，不表示全部记忆都应该被读取。

内容召回沿着另一条链路执行：模型从当前任务提炼主题与检索词；脚本返回候选文件和少量命中片段；模型选择最相关的最小候选集；脚本精确读取指定文件的必要行段；上下文足够后停止继续读取。

```text
主题与检索词
      ↓
受限候选片段
      ↓
最小候选集
      ↓
精确行段
      ↓
信息足够，停止读取
```

这里的自由度划分来自任务性质。路由是否成立、候选是否真正相关、冲突应该如何解释，这些问题依赖语义，由模型处理。文件搜索、路径限制、行段读取、格式转换和写入不需要语言推理，由脚本完成。模型负责语义控制面，脚本负责确定性数据面，两者通过明确输入输出交接。

“信息足够”是读取的最终停止条件。“还可以继续读”本身不是理由，继续读取必须能明确补足某个尚未解决的问题。最小相关集也不是固定文件数量，而是完成当前任务所需的最少证据集合。

契约采用语义化版本。路由语义或输入输出发生不兼容变化时提升 MAJOR；增加兼容操作或字段时提升 MINOR；不改变契约的脚本修正和措辞澄清提升 PATCH。每次调整路由，都需要同步检查 `AGENTS.md` 与 `SKILL.md`，保证高优先级入口和完整执行契约使用同一套语义。

---

## 六、完整闭环把记忆放回知识固化体系

保存、召回和维护是三条独立路径。保存为未来提供可召回状态，但不会自动触发读取；读取由未来任务的历史依赖决定；长期记忆的形成和退出则由独立的晋升与维护判断完成。三条路径共享数据，却不共享权限。

```text
当前任务到达
    │
    ├─ 不依赖历史
    │      └─ 零记忆 I/O，直接完成任务
    │
    ├─ 明确依赖历史
    │      └─ 路由通过 → 主题检索 → 最小读取 → 完成任务
    │
    └─ 形成有未来延续价值的交付
           └─ 只依据当前会话写入 daily
                    ↓
               后续协作继续验证
                    ↓
             新增 / 更新 / 删除 / 忽略
                    ↓
              必要时维护 MEMORY.md
```

完整迁移需要同时带走三类资产：`MEMORY.md`、daily 和历史快照组成的数据；负责全局路由的 `AGENTS.md`；包含完整契约、脚本和元数据的 `memory-system` Skill。数据提供可以召回的历史，`AGENTS.md` 提供服务入口，Skill 提供稳定执行方式。纯文本数据保持平台无关，Skill 中与运行环境相关的脚本可以按目标系统替换，而不改变数据语义和路由契约。

记忆系统同时处在 Harness、记忆、Skill 三层知识固化体系的中间。Harness 固化单个项目内部的架构、约束、执行计划、运行日志和验证闭环，解决 Agent 进入某个仓库后应该知道什么。记忆系统固化跨会话、跨项目仍然具有价值的协作状态，解决过去发生过什么，以及当前任务是否需要把那段过去重新带回来。Skill 固化可复用工作流，解决遇到某类任务时应该按照什么契约执行；读取和维护记忆的标准流程本身，因此被封装为 `memory-system` Skill。

三者按作用域和加载时机区分。某个仓库的构建命令属于 Harness；跨项目适用的沟通偏好属于记忆；如何按主题最小读取记忆属于 Skill。Harness 按项目加载，Skill 按任务类型触发，记忆按历史依赖和主题相关性召回。

它们共享同一个内核：把隐性的、易丢失的经验，转成显性的、有结构的、可被后续 Agent 消费的资产。知识固化同时需要控制加载边界，确保每项知识只在正确作用域和正确任务中出现。

## 结语：保存完整历史，只召回必要部分

一套成熟的记忆系统需要实现三个结果：应该保存的状态不会消失，当前任务真正需要的历史能够被找到，不相关的过去不会进入当前上下文。

文件系统提供透明、可迁移的事实源；长期认知、daily 和历史快照分担不同生命周期；路由门禁决定是否读取；渐进召回决定读取多少；四种操作隔离读取、写入、晋升和维护权限；冲突规则确保记忆始终服从当前现实。

这些机制共同把一组 Markdown 文件组织成一项有明确边界的跨会话状态服务。它保存完整历史，但只为当前任务召回必要部分。
