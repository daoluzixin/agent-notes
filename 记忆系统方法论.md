# 记忆系统是如何构建的

## 一句话内核

这套系统要解决的,是 AI 协作里一个绕不开的硬伤:**失忆**。每开一个新会话,模型的上下文都是空的——前面所有的协作、决策、踩过的坑、对齐过的偏好,全部归零。你跟它聊一周建立起来的默契,第八天开个新窗口就荡然无存。记忆系统就是用文件系统这个最朴素的持久化手段,把这些"会丢的东西"以结构化方式存下来,让每个新会话开局就带着历史上下文,而不是每次从零开始重新介绍自己。

它跟编码用的 harness 是同构的:harness 让单个项目内部的知识有结构地沉淀,记忆系统让跨会话的知识有结构地沉淀。两者共用一个内核——把隐性的、容易丢失的经验,转成显性的、有结构的、可被复用的资产。

整套系统就两样东西:一份**数据**(磁盘上的 Markdown 文件),一套**驱动**(写在 `AGENTS.md` 里、让模型每次照做的指令)。下面分四层讲数据怎么组织,再讲驱动怎么把它转起来。

## 第一层:磁盘就是全部

这套系统的存储只有一处:实打实落在磁盘上的 Markdown 文件。没有数据库、没有服务、没有任何需要联网的东西——它就是几个纯文本文件,通过普通的文件读写来访问。

这个朴素到极致的选择,恰恰是它最大的优点。Markdown 文件可以用任何编辑器打开、可以人肉改、可以 diff、可以丢进 git 做版本管理、可以直接拷到另一台机器上接着用。它不绑定任何特定的 AI 客户端或平台:今天在这台电脑的 codex 上跑,明天整个目录拷到另一台机器,换个工具,记忆原封不动地跟过去。只要文件系统在,记忆就在。

代价是检索方式很朴素——没有什么"语义模糊匹配",找历史全靠两招:**按文件名定位**(daily 文件就是日期,知道大概什么时候发生的,直接翻对应那天)和**全文检索**(grep 或编辑器搜索,按关键词扫)。但这点"笨"换来的是彻底的可移植和零依赖,在一个只有文件系统、没有任何记忆基础设施的环境里,这是唯一能稳定工作的形态——把鸡蛋放在最不会消失的篮子里。

## 第二层:长期 / 每日的时间分层

这是整个数据结构的骨架,落到磁盘上非常简单:一个 `MEMORY.md`,加上一个 `daily/` 目录,里面是一堆 `YYYY-MM-DD.md`。截至目前这套系统已经跑了一年多,daily 攒了 60 个文件——它不是设想,是个活系统。

两层的切分标准不是"重要 / 不重要",而是**时效性**。判断一条信息该进哪层,靠三个连环追问:

1. **时效性测试**——这事 30 天后还成立吗?
2. **变化频率测试**——它多久会变一次?
3. **跨会话价值测试**——一个全新的会话需不需要知道它?

`MEMORY.md` 装的是"你是谁":稳定的身份、编码偏好、技术栈选择、重大架构决策、行文风格。这些东西几个月都不带变的,所以它配当"常驻背景"——每个会话开局就整段读进上下文。

`daily/` 装的是"你在干什么":某天在调的 bug、当前任务进度、会话纪要。这些天然会过期、会完成,所以按日期归档,需要时再按日期翻出来,而不是常驻占用宝贵的上下文窗口。

这里有个很值得拿出来说的设计:**主表 + 索引**。各个项目(git-ai、MiniResearcher、各种业务仓库分析)的局部知识,全都沉在对应日期的 daily 里,`MEMORY.md` 里只留一张索引表指向它们——"某项目的细节在 daily/2026-05-12"。这么做的好处是,常驻上下文始终保持精简,但又一条细节都不丢。类似图书馆不会把所有书都摆在门口,门口只放一个检索目录,真要看哪本再去对应书架取。如果不做这层索引,要么 MEMORY.md 膨胀到几千行、每次开局加载都拖慢一截,要么细节散在 daily 里再也找不回来——两边都是灾难。

## 第三层:晋升机制

光有分层还不够,分层是静态的,系统需要一套机制让知识在两层之间**流动**起来,否则它很快会僵死。

核心规则是:**不确定就先写 daily,验证过了再晋升到长期**。一条新沉淀的知识,默认先进 daily 当"观察期样本"。等它在后续协作里被反复验证、确认确实是稳定可复用的,才提拔进 `MEMORY.md`。这是个典型的"灰度发布"思路——新东西先放在影响面小的地方观察,跑稳了再推到全量(全量在这里就是"每个会话都加载")。

流动是双向的。长期记忆不是只进不出——它要被**定期审视**,已经失效的项目信息、已经过时的工具版本知识,该降级降级、该删删。配套的还有一条写入纪律:每个写入决策都走"**新增 / 更新 / 删除 / 忽略**"四选一,不是无脑往里堆。

这套"晋升 + 审视 + 四选一"合起来,本质上是在给记忆做垃圾回收。没有它,记忆库会像一个从不清理的下载文件夹,迟早膨胀成一堆过期噪音,真有用的信息反而被淹没。一个会膨胀但不会收缩的系统,寿命是有限的。

## 第四层:写入纪律

前面三层都是"结构",这一层是保证结构能持续运转的"输入管道"。

铁律只有一条:**每轮交付后必须把会话纪要写进 daily,只有纯确认性的单轮问答才豁免**。这条纪律看着简单,但它是整个系统的生命线——daily 是记忆库唯一的输入来源,管道一停,记忆就开始断流、失真,几天后再想回溯就会发现中间空了一大段。坚持写,才能保证后面晋升机制有料可提、检索时有据可查。

## 驱动:靠 AGENTS.md 把系统转起来

到这里数据结构讲完了,但数据是死的——MEMORY.md 不会自己被读、daily 不会自己被写、知识不会自己晋升。这套系统能转,靠的是另一半:**驱动**。

驱动的载体是项目根目录的 `AGENTS.md`。模型每个会话都会读它,所以把记忆系统的操作约定写进去,就等于给模型下了一道每次都生效的常驻指令。需要在 `AGENTS.md` 里写死三条:

1. **开局先读**——会话一开始,先读 `MEMORY.md`(常驻身份),再扫一眼最近几天的 `daily/`(近期上下文),把历史捞回来再开始干活。`AGENTS.md` 里要明确指出这两个路径,模型才知道去哪儿读。
2. **交付后写**——每轮实质性交付结束,把会话纪要追加进 `daily/今天.md`,纯确认性问答豁免。这是输入管道的闸门,不写就断流。
3. **验证后晋升**——稳定可复用的知识从 daily 提到 `MEMORY.md`,过期的定期清理,每次决策走"新增 / 更新 / 删除 / 忽略"四选一。

这三条对应的正是前面四层的运转逻辑:开局先读 = 激活时间分层里的常驻与近期;交付后写 = 写入纪律;验证后晋升 = 晋升机制。数据结构定义了"知识长什么样",`AGENTS.md` 定义了"知识怎么流动",两者合起来才是一台完整运转的机器。

## 把整套连起来:完整的运作闭环

```
一轮会话发生
   │
   ▼
纪要写进当天 daily            ← 写入纪律(AGENTS.md 第 2 条)
   │
   ▼
知识在 daily 沉淀、被后续协作观察验证
   │
   ▼
验证通过 → 晋升进 MEMORY.md    ← 晋升机制(AGENTS.md 第 3 条)
   │
   ▼
下个会话开局
   ├─ 读取 MEMORY.md          ← 常驻 = 你是谁
   └─ 按需:全文检索 / 翻对应 daily   ← 近期上下文
   │   (开局先读,AGENTS.md 第 1 条)
   ▼
新会话又产生新纪要 → 回到顶端
```

这个闭环跑通之后,跨会话的协作就不再是每次从零开始,而是站在所有历史积累之上往前走。

## 迁移到新机器

整套系统的可移植性,正源于它"数据 + 驱动"都是纯文本这一点。搬到一台新电脑只有两步:

第一,**搬数据**——把 `MEMORY.md` 和整个 `daily/` 目录拷到新机器的项目里。这是一年多积累的全部家底,拷过去就在。

第二,**搬驱动**——确保项目根目录的 `AGENTS.md` 里带着上面那三条指令。模型读到它,就会在新环境里自动恢复"开局读、交付写、验证晋升"的节奏。

两步做完,系统在新机器上原样复活,不丢任何历史、不依赖任何外部服务。

## 它和你其他工作习惯的关系

记忆系统不是孤立的,它是你"三层知识固化"方法论里的一支:

- **harness** 固化单个项目内部的结构化知识(AGENTS.md 入口地图、docs/agents/ 知识文档、exec-plans 执行记录、规则文件、自动化守卫)。
- **记忆系统** 固化跨会话的协作上下文(就是本文讲的这套)。
- **skill** 固化可复用的工作流(把重复性、有固定套路的任务固化成可调用、可定时触发的 Skill)。

三者共用同一个内核:把隐性的、易丢失的经验,转成显性的、有结构的、可复用的资产。这也跟你做事一贯的"系统性、可证伪、事后复盘"倾向对得上——先立结构,再执行,最后回头审视修正。
